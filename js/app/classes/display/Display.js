define(['Jquery', 'Class'], function($, Class){
	
	var canvas, title, width, height, graphics;
	
	var Display = Class.extend({
		init:funtion(_title, _width, _height){
			title = _title;
			width = _width;
			height = _height;
		}
	});
	
});